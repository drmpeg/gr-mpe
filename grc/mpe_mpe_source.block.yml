# auto-generated by grc.converter

id: mpe_mpe_source
label: IP over TS Packet Source
category: '[IP over TS (MPE)]'

parameters:
-   id: mac_address
    label: MAC Address
    dtype: string
-   id: ping_reply
    label: Ping Reply
    dtype: enum
    options: [PING_REPLY_OFF, PING_REPLY_ON]
    option_labels: ['Off', 'On']
    option_attributes:
        val: [mpe.PING_REPLY_OFF, mpe.PING_REPLY_ON]
-   id: ipaddr_spoof
    label: UDP IP Address Spoofing
    dtype: enum
    options: [IPADDR_SPOOF_OFF, IPADDR_SPOOF_ON]
    option_labels: ['Off', 'On']
    option_attributes:
        hide_ipaddr: [all, '']
        val: [mpe.IPADDR_SPOOF_OFF, mpe.IPADDR_SPOOF_ON]
-   id: src_address
    label: Source IP Address
    dtype: string
    hide: ${ ipaddr_spoof.hide_ipaddr }
-   id: dst_address
    label: Destination IP Address
    dtype: string
    hide: ${ ipaddr_spoof.hide_ipaddr }

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import mpe
    make: mpe.mpe_source(${mac_address}, ${ping_reply.val}, ${ipaddr_spoof.val}, ${src_address},
        ${dst_address})

file_format: 1
